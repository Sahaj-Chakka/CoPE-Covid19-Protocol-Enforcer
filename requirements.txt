# CoPE — Covid19 Protocol Enforcer
# Python dependencies
# Install: pip install -r requirements.txt

# Computer Vision
opencv-python>=4.5.0
opencv-contrib-python>=4.5.0
numpy>=1.21.0

# Facial Recognition
face-recognition>=1.3.0
dlib>=19.22.0

# Raspberry Pi Hardware
RPi.GPIO>=0.7.0
smbus2>=0.4.1

# OLED Display
adafruit-circuitpython-ssd1306>=2.12.0
adafruit-blinka>=8.0.0
Pillow>=9.0.0

# Weather API
requests>=2.27.0

# Email
# (smtplib is built-in to Python, no install needed)

# Optional: TensorFlow Lite for mask model (lighter than full TF on Pi)
# tflite-runtime>=2.9.0

# Optional: Full TensorFlow (heavy — use TFLite on Pi instead)
# tensorflow>=2.9.0
